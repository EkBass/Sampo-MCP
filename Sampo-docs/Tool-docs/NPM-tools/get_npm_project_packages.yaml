# TOOL DOCUMENTATION: sampo:get_npm_project_packages
tool_description: List npm packages installed in current project directory with versions and dependency trees.
returns:
    type: object
    structure: Complete project dependency tree with versions
example:
    - 'sampo:get_npm_project_packages()'